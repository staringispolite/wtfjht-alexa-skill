'use strict';

module.exports.parse = function parseString(_ref, next) {
	var feed = _ref.feed;
	var data = _ref.data;

	// Detect
	var isText = require('istextorbinary').isTextSync(feed.basename, data);
	if (!isText) {
		next();
	} else {
		// Parse
		next(null, data.toString());
	}
};