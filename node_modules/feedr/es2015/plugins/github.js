'use strict';

module.exports.check = function checkGitHub(_ref, next) {
	var feed = _ref.feed;
	var data = _ref.data;

	if (feed.url.indexOf('github.com') !== -1 && data && data.message) {
		return next(new Error(data.message));
	}
	return next();
};